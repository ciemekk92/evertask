import styled from 'styled-components';
import { StyledSectionWrapper } from 'Shared/PageWrappers';

export const StyledSprintWrapper = styled(StyledSectionWrapper)`
  width: 98.5%;
`;

export const StyledLabel = styled.strong`
  font-size: 1.4rem;
`;
