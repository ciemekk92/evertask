import styled from 'styled-components';
import {
  StyledFlexColumnContainer,
  StyledFlexContainerSpaceBetween
} from 'Shared/SharedStyles.styled';

export const StyledFieldContainer = styled(StyledFlexColumnContainer)`
  font-size: 1.6rem;
  padding: 1rem 2rem;
`;

export const StyledTextContainer = styled(StyledFlexContainerSpaceBetween)`
  margin-bottom: 0.5rem;
`;
