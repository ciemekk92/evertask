import styled from 'styled-components';
import { StyledFlexContainer } from 'Shared/SharedStyles.styled';

export const StyledHeadingContainer = styled(StyledFlexContainer)`
  margin-bottom: 3rem;
`;
